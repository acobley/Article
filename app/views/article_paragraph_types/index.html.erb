<h1>Listing article_paragraph_types</h1>

<table>
  <tr>
    <th>Descriptio</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @article_paragraph_types.each do |article_paragraph_type| %>
  <tr>
    <td><%= article_paragraph_type.Descriptio %></td>
    <td><%= link_to 'Show', article_paragraph_type %></td>
    <td><%= link_to 'Edit', edit_article_paragraph_type_path(article_paragraph_type) %></td>
    <td><%= link_to 'Destroy', article_paragraph_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Article paragraph type', new_article_paragraph_type_path %>
